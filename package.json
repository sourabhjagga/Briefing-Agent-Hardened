{
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.10.10",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "sharp": "0.35.4",
    "puppeteer-core": "25.8.0"
  }
}
